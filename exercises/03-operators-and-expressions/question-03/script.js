// Declaramos duas variáveis com valores diferentes
let a = 5;
let b = 7;

// Comparações
console.log("a == b:", a == b);           // false
console.log("a === b:", a === b);         // false
console.log("a > b:", a > b);             // false
console.log("a <= b:", a <= b);           // true