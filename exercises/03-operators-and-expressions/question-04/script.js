// Três variáveis booleanas
let A = true;
let B = false;
let C = true;

// Expressões lógicas
console.log("A && B:", A && B);  // false (true e false = false)
console.log("A || C:", A || C);  // true (true ou true = true)
console.log("!C:", !C);          // false (negação de true)