let idade = 20;
let temDocumento = true;

// Verificando se pode entrar no evento
if (idade >= 18 && temDocumento) {
    console.log("Entrada permitida.");
} else {
    console.log("Entrada negada.");
}