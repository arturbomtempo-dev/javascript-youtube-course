// Criando um vetor com 3 cidades
const cidades = ['Belo Horizonte', 'Curitiba', 'Recife'];

// Substituindo a cidade da posição 1 por "São Paulo"
cidades[1] = 'São Paulo';

// Exibindo o vetor atualizado no console
console.log(cidades);
