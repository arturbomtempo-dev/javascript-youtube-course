// Criando um vetor vazio chamado 'numeros'
const numeros = [];

// Adicionando 3 números manualmente em cada posição
numeros[0] = 10;
numeros[1] = 20;
numeros[2] = 30;

// Calculando a soma dos valores armazenados no vetor
const soma = numeros[0] + numeros[1] + numeros[2];

// Exibindo a soma no console
console.log(soma);
